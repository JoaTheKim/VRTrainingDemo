namespace VRTraining.ValveInteraction
{
    /// <summary>
    /// Local axis on the wheel transform that the valve rotates around.
    /// </summary>
    public enum ValveRotationAxis : int
    {
        X = 0,
        Y = 1,
        Z = 2,
    }
}
